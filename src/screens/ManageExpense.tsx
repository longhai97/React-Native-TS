import React from "react";
import { View, Text, StyleSheet } from "react-native";

const ManageExpenseScreen = () => {
  return (
      <View>
        <Text>Manages Expenses Screen</Text>
      </View>
  );

};

const styles = StyleSheet.create({});

export default ManageExpenseScreen;
